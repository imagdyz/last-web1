# Copy to .env.production before `npm run build` (optional).
# If unset, the app uses the same origin: https://YOUR-DOMAIN/backend/api

# Full API base URL (no trailing slash)
# VITE_API_URL=https://example.com/backend/api

# If the site lives in a subfolder, set base path (default /)
# VITE_BASE_PATH=/
